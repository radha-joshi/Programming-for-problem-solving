// C program to demonstrate if else statement
#include <stdio.h>
int main()
{
    int num = 10;
    if (num%2==0)
        printf("%d is even", num);
    else
        printf("%d is odd", num);
    return(0);
}